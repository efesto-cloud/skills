# Local development environment — loaded via @dotenvx/dotenvx by `pnpm dev`.
# Values here are safe defaults for the devcontainer; override as needed.

PORT=3000
LOG_LEVEL=info
NODE_ENV=development
ROOT_URL=http://localhost:3000

MONGO_DB_URL_PATH=./secrets/mongo_url
COOKIE_SECRET_PATH=./secrets/cookie_secret

VERSION=0.0.1
BUILD_DATE=
