// Cookies facade — stub. When `auth_mode == "operator-session"` the scaffolder
// replaces this file with a wired version that defines `operator_cookie`.
//
// TODO(scaffold): pick an auth strategy and define cookies here. The Remix
// pattern is:
//   import { createCookie } from "react-router";
//   export const mySessionCookie = createCookie("my_cookie", { ... });

export {};
